import React from 'react';
import { createRoot } from 'react-dom/client';
import App5 from './componentes/Demo5/App5';

const container=document.getElementById('root');
const root = createRoot(container);

root.render(
    <App5/>
);

